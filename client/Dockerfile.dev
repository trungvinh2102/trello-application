FROM node:20-alpine

WORKDIR /app

# Install dependencies first for better caching
COPY package.json yarn.lock ./
RUN yarn install

# Copy source code
COPY . .

# For development, we don't build, just keep dependencies ready
CMD ["sh"]
